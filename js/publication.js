class Publication{

    constructor(options){

        this.authorsList = options.authorsList;
        this.title = options.title;
        this.details = options.details;
        this.year = options.year;
        this.abstract = options.abstract;
        this.link = options.link;
    }
}